<table class="table">
  <tr>
    <th>Name</th>
    <th>Username</th>
    <th>Cohort</th>
  </tr>
  <% @students.each do |student|%>
  <tr>
    <th><%= student.name %></th>
    <th><%= student.username %></th>
    <th><%= student.cohort %></th>
  </tr>
  <% end %>
</table>
